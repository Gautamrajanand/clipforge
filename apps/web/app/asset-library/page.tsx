'use client';

import { UpgradePrompt } from '@/components/ui/UpgradePrompt';

export default function AssetLibraryPage() {
  return (
    <UpgradePrompt
      feature="Asset Library"
      description="Store and manage all your brand assets in one place. Upload fonts, images, videos, audio files, and create censored words lists for your content."
    />
  );
}
