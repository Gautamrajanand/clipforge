'use client';

import { UpgradePrompt } from '@/components/ui/UpgradePrompt';

export default function BrandTemplatePage() {
  return (
    <UpgradePrompt
      feature="Brand Template"
      description="Create custom brand templates with your logo, colors, fonts, and media assets. Apply consistent branding across all your content with one click."
    />
  );
}
